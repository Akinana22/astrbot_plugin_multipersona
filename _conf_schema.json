{
  "idle_timeout_minutes": {
    "type": "int",
    "description": "空闲超时",
    "hint": "超时后按权重随机唤醒人格。设为 0 则禁用空闲切换。",
    "default": 30
  },
  "consent_timeout_rounds": {
    "type": "int",
    "description": "确认超时",
    "hint": "等待人格/用户确认的最大对话轮数。超时取消切换。",
    "default": 3
  },
  "auto_create_personas": {
    "type": "bool",
    "description": "自动创建人格",
    "hint": "插件初始化时自动将人格数据写入 AstrBot Persona 数据库",
    "default": true
  },
  "stop_event_after_switch": {
    "type": "bool",
    "description": "阻止转发",
    "hint": "切换成功后阻止触发切换的用户消息继续传给 LLM",
    "default": true
  }
}
